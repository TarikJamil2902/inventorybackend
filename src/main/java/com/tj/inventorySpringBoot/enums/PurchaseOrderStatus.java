package com.tj.inventorySpringBoot.enums;

public enum PurchaseOrderStatus {
    PENDING,      // Order is created but not processed
    COMPLETED,    // Order is successfully received and completed
    CANCELLED     // Order is cancelled
}
